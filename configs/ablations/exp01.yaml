# Exp 01 — DiT-XL ULTIMATE SOTA (burn most of remaining budget)
slurm:
  node: h100-80
  gpus: 8
  time: "09:00:00"
  # wall-clock 估算: DiT-XL Ada75% @ H100 0.15 GPUh/ep, 400ep × 0.15 / 8 × 1.2 = 9h

framework: flow_matching
use_ot: true
use_cfg: true
model_type: dit_xl
use_ada_mask: true
ada_mask_max: 0.75
num_epochs: 400
batch_size: 16
learning_rate: 0.0003
