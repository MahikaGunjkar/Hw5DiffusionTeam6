# Exp 20 — DDPM baseline saturation at 200 epochs
slurm:
  node: v100-32
  gpus: 4
  time: "20:00:00"

use_cfg: true
framework: ddpm
model_type: unet
num_epochs: 200
batch_size: 32
learning_rate: 0.0001
