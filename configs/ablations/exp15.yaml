# Exp 15 — RF + OT on UNet (RF gain without DiT)
slurm:
  node: v100-32
  gpus: 4
  time: "08:00:00"

framework: flow_matching
use_cfg: true
model_type: unet
num_epochs: 50
batch_size: 32
learning_rate: 0.0001
use_ot: true
