# Exp 04 — DiT-S Scaling Law lower point
slurm:
  node: v100-32
  gpus: 8
  time: "16:00:00"

framework: flow_matching
use_ot: true
use_cfg: true
model_type: dit_s
use_ada_mask: true
ada_mask_max: 0.75
num_epochs: 300
batch_size: 64
learning_rate: 0.0004
