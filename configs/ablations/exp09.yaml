# Exp 09 — No Mask (overfitting evidence)
slurm:
  node: h100-80
  gpus: 8
  time: "03:00:00"
  # wall-clock 估算: DiT-B no_mask @ H100 0.06 GPUh/ep, 300ep × 0.06 / 8 × 1.2 = 2.7h → 3h

framework: flow_matching
use_ot: true
use_cfg: true
model_type: dit_b
use_ada_mask: false
num_epochs: 300
batch_size: 32
learning_rate: 0.0003
